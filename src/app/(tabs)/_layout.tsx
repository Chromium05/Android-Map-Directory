// The tab bar itself lives in `@/components/app-tabs` so the native and web
// variants can resolve via platform-specific files (app-tabs.tsx / .web.tsx).
export { default } from '@/components/app-tabs';
